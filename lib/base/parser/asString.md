# $lib/base/parser.ql: asString

Function `asString` used to parse `object` to string

Signature:

```ql
function asString(let object)
```

Example (will return String type):

```ql
let var = 55;
Parser.asString(var);
```
