# $lib/base/parser.ql: asNumber

Function `asNumber` used to parse `object` to number

Signature:

```ql
function asNumber(let object)
```

Example (will return Number type):

```ql
Parser.asNumber("55");
```
