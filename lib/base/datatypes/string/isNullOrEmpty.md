# $lib/base/datatypes/string: isNullOrEmpty

Function `isNullOrEmpty` used to check if string is null or empty
Return type: `bool`

Signature:

```ql
function isNullOrEmpty(let str)
```

Example:

```ql
// Will return false
String.isNullOrEmpty("not empty and not null");
```
