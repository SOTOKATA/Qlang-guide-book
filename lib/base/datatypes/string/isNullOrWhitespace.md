# $lib/base/datatypes/string: isNullOrWhitespace

Function `isNullOrWhitespace` used to check if string is null or is whitespace (" ")
Return type: `bool`

Signature:

```ql
function isNullOrWhitespace(let str)
```

Example:

```ql
// Will return true, because string is whitespace
String.isNullOrWhitespace(" ");
```
