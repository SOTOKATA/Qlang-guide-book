# $lib/base/datatypes/string: length

Function `length` used to get string length
Return type: `Number`

Signature:

```ql
function length()
```

Example:

```ql
let str = String.new("Hello ");

// Will return '6'
str.length();
```
