# $lib/base/datatypes/string: new

Function `new` used to create class instance
Return type: `String`

Signature:

```ql
function new(let string)
```

Example:

```ql
let str = String.new("Hello World!");
```
