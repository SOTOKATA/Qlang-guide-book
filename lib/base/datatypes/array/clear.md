# $lib/base/datatypes/array: clear

Function `clear` used to clear collection
Return type: `void`

Signature:

```ql
function clear()
```

Example:

```ql
let arr = Array.new([1]);

// Collection is '[]'
arr.clear();
```
