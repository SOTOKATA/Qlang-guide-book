# $lib/base/datatypes/array: setAt

Function `removeAt` used to remove item by index
Return type: `void`

Signature:

```ql
function removeAt(let index)
```

Example:

```ql
let arr = Array.new([1, 5]);

// Collection is '[5]'
arr.removeAt(0);
```
