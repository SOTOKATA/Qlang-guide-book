# $lib/base/datatypes/array: contains

Function `contains` used to get Array collection
Return type: `bool`

Signature:

```ql
function contains(let item)
```

Example:

```ql
let arr = Array.new([1]);

// Will return true
arr.contains(1);
```
