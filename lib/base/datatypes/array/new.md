# $lib/base/datatypes/array: new

Function `new` used to create new class instance
Return type: `Array`

Signature:

```ql
function new(let collection)
```

Example:

```ql
let arr = Array.new([1]);
```
