# $lib/base/datatypes/array: getCollection

Function `getCollection` used to get Array collection
Return type: `collection`

Signature:

```ql
function getCollection()
```

Example:

```ql
let arr = Array.new([1]);

// Will return '[1]'
arr.getCollection();
```
