# $lib/base/datatypes/array: isArray

Function `isArray` used to detect if is collection
Return type: `bool`

Signature:

```ql
function isArray(let collection)
```

Example:

```ql
// Will return true
Array.isArray([1]);

// Will return false
Arrat.isArray(" ");
```
