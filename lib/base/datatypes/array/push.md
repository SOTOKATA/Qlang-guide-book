# $lib/base/datatypes/array: push

Function `push` used to add item to Array
Return type: `void`

Signature:

```ql
function push(let item)
```

Example:

```ql
let arr = Array.new([]);

// Will add '1' to collection
arr.push(1);
```
