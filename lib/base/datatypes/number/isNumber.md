# $lib/base/datatypes/number: isNumber

Function `isNumber` used to detect if string is number
Return type: `bool`

Signature:

```ql
function isNumber(let string)
```

Example:

```ql
// Will return true
Number.isNumber("2");
```
