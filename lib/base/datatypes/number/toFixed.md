# $lib/base/datatypes/number: toFixed

Function `toFixed` used to change number view
Return type: `string`

Signature:

```ql
function toFixed(let number, let pattern)
```

Example:

```ql
// Will return '3.1'
Number.toFixed(3.15, "0.0");
```
