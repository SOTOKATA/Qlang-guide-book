# $lib/base/datatypes/number: randInt

Function `randInt` used to get random integer numbers
Return type: `Number`

Signature:

```ql
function randInt(let min, let max)
```

Example:

```ql
// Will return Number in range: 3-10
Number.randInt(3, 10);
```
