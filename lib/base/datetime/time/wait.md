# $lib/base/datetime/time: wait

Function `wait` used to make program sleeping

Signature:

```ql
function wait(let milliseconds)
```

```ql
// 1000 ms = 1 sec
// Program will stop and 'sleep' during 1 second
Time.wait(1000);
```
