# $lib/base/console.ql: setBackColor

Function `setBackColor` used to set console's background color
Available colors is similar to C# colors

Signature:

```ql
function setBackColor(let color)
```

Example:

```ql
Console.setBackColor("yellow");
```
