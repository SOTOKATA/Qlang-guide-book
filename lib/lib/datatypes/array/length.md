# $lib/base/datatypes/array: length

Function `length` used to get collection length
Return type: `Number`

Signature:

```ql
function length()
```

Example:

```ql
let arr = Array.new(["Hello World!", 5]);

// Will return '2'
arr.length();
```
