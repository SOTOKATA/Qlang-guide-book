# $lib/base/console.ql: setForeColor

Function `setForeColor` used to set console's foreground color
Available colors is similar to C# colors

Signature:

```ql
function setForeColor(let color)
```

Example:

```ql
Console.setForeColor("yellow");
```
