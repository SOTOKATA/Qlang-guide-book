# $lib/base/console.ql: clear

Function `clear` used to clear console

Signature:

```ql
function clear()
```

Example:

```ql
Console.clear();
```

Console (until clearing):

```ql
Hello World!
Write name:

```

Console (after clearing):

```text
```
