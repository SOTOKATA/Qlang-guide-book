# $lib/base/console.ql: resetColors

Function `resetColors` used to return to default console's colors
Default is gray color for foreground and black color for background

Signature:

```
function resetColors()
```

Example:

```
Console.resetColors();
```