# $lib/base/console.ql: cursorVisible

Function `cursorVisible` used to set cursor visible

Signature:

```ql
function cursorVisible(let visible)
```

Example:

```text
Console.cursorVisible(false);
```
